name: plugs
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  meta: ^1.7.0
  logger: ^1.1.0
  http: ^0.13.4
  path: ^1.8.1

dev_dependencies:
  flutter_lints: ^1.0.0
  test: ^1.16.0
